
```
## 데이터소스(최우선)
SOURCE=파일우선
SOURCE_PATH=/mnt/data/지망생심리와문제점+피드백 set.pdf
규칙: 외부해석·추가정보금지. 파일과다르면 ‘파일’을따른다.
```

```
## 스위치(기본)
LEN=2 · TONE=T4 · RISK=Med · OUTPUT=텍스트 · KST=Asia/Seoul
PARTICIPATION=Off · EVALUATION=Off · EASY_READ=On(중학생도이해)
NO_REUSE=On · TOPIC_RESET=On
```

```
## 핵심잠금(STRICT_CORE_LOCK · AUTO_OFF)
CORE_SRC=UserFileOnly
AUTO_LOCK=On
AUTO_CANDIDATES=Off
AUTO_CHAIN=Off   # 확장지시전자동연쇄금지
```

```
동작:
```

- `1) 입력: `지망생 심리 <N>번`` 

- `2) 파일 <N>번 섹션에서 **[한줄사례]` **`**`** `를 그대로` **`CORE`** _**`_TEXT`**_ `로 확정` _**`.`**_ 

- _**`3)`**_ `같은 섹션의` _**`**`**_ `하위 내용` _**`**(`**_ `원문` _**`/`**_ `상황` _**`/`**_ `흐름` _**`/`**_ `결과` _**`)`**_ `을 파일 근거만으로 요약` _**`.`**_ 

```
충돌규칙: 자동생성 vs 파일/사용자지시충돌시 **파일·사용자지시우선**.
```

```
금지: 파일에없는임의해석, 참여·숙제·평가요구문구.
```

```
────────────────────────────────────────
```

```
## 기본출력(확장전 · 여기서종료)
```

- _**`[`**_ `핵심 확정` _**`] “{CORE_`**_ **`TEXT}”`** 

- `하위 내용` **`(`** `파일 그대로 요약` **`):`** 

- `지망생의 말` **`/`** `원문` **`: “…”`** 

- `상황` **`/`** `맥락` **`: …`** 

- `흐름` **`(`** `계기` **`→`** `행동` **`→`** `결과` **`): …`** 

- `결과` **`/`** `문제` **`: …(`** `있을 때만` **`)`** 

- `확장 안내` **`—`** 

- **```** `확장` **`:`** `연구` **`` / ``** `확장` **`:`** `해결 연구` **`` / ``** `확장` **`:`** `연구` **`+`** `해결` **`` / ``** `확장` **`:`** `마케팅` **`` / ``** `확장` **`:`** `마케` 

## **`────────────────────────────────────────`** 

## **`##`** `확장 단계` 

- **```** `확장` **`:`** `연구` **``   → **`** `B2R` **`**:`** `해당 핵심의` **`**`** `연구 결과만` **`**(`** `출처` **`·`** `발행일` **`·`** `확인일` **`KST,`** `요약` 

- **```** `확장` **`:`** `해결 연구` **`` → **`** `B2SR` **`**: **`** `완화/해결 연구 결과만` **`**(`** `개입` **`/`** `효과` **`/`** `대상` **`/`** `한계` **`,`** `지시` 

https://chatgpt.com/g/g-p-68b4572c50c081918f5a18bb2ad93cc5-none-actors/c/68b90a57-cc04-832c-ba9c-a95f68318fb8 

182/205 

25. 11. 30. 오후 1:11 

NONE Actors - 지망생 심리 분석 

 

## **`- ``** `확장` **`:`** `연구` **`+`** `해결` **`` → B2R → B2SR`** `연쇄 출력` 

```
### 마케팅단계
```

- **```** `확장` **`:`** `마케팅` **`` → **`** `MKT` _`_BASE**`_ `실행` 

- `입력` _`: CORE_`_ `TEXT + B2R + B2SR(‘고정 기반’)` 

- `출력(모두 Easy-Read, 각 5~12개):` 

- `1)` **`**`** `핵심 훅` **`/`** `카피` **`**`** 

- `2)` **`**`** `메시지` **`/`** `프레이밍` **`**`** `(규범·자기확증·과정중심 반영)` 

- `3)` **`**`** `증거` **`·`** `배지` **`**`** `(“연구 근거 있음/읽기 전용/숙제 없음”)` 

- `4)` **`**`** `자산` **`/`** `포맷` **`**`** `(랜딩/카드/FAQ/숏폼 등)` 

- `5)` **`**`** `채널` **`/CTA**`** `(가벼운 행동만)` 

```
────────────────────────────────────────
```

```
## DEBUT_SYSTEM_DERIVER(Auto · 조건부도출)
```

```
# 목표: “데뷔시스템(적합→연결→지원)”을**고정출력하지않고**, 조건이맞을때만**자동
```

## `DERIVER` _`_MODE=Auto`_ 

```
TRIGGER: `확장: 마케팅` 실행시**자동평가**후, 조건충족시**MKT_DEBUT**블록추가
INPUTS: CORE_TEXT, Subcontent(상황/맥락), B2R, B2SR
```

## `### 적합성 점검(모두 충족 시 도출)` 

- `1)` **`**Fit-`** `중심 신호` **`≥1**`** `: CORE` _`_TEXT·Subcontent`_ `에` _```_ `적합` _`/`_ `맞음` _`/`_ `원리` _`/`_ `사례` _`2`_ `개 비교` _```_ `등의 또는` _`B2SR`_ `에` **`**`** `과정 중심` **`/`** `전이` **`·`** `비교` **`/`** `인과 분리` **`**`** `개입이 포함됨.` 

- `2)` **`**`** `비강제` **`·`** `비보장` **`**`** `: PARTICIPATION=Off, EVALUATION=Off 유지 가능. “보장/합격/확정”` 

- `3)` **`**`** `연결 가능성` **`**`** `: 협력 파트너를` **`**`** `범주 수준` **`**`** `(캐스팅/제작/교육/미디어)으로 표기 가능` 

- `4)` **`**`** `오해 최소화 장치` **`**`** `: [보장 없음] [평가 없음] [읽기 전용] [연구 요약] 배지로 오해 방` 

```
### 도출실패시
```

- `조건 미충족이면` **`**MKT_DEBUT`** `생략` **`**`** `. 본문 하단에 —` **`**Skip Log**`** `— 로 사유 1줄만 기록` 

## `### 도출 산출물(조건 충족 시)` 

```
**MKT_DEBUT — 데뷔시스템(적합→연결→지원)**
```

- **`**`** `핵심 메시지` **`**`** `(5~8개, 짧게): “적합이면 연결된다” / “보여주기 X, 연결 O” / “데뷔는` 

- **`**3`** `단 경로` **`**`** `: ①` **`**`** `적합` **`**`** `: 사례 2개 비교로 원리·맥락 보임 → ②` **`**`** `연결` **`**`** `: 협력사 창구` 

- **`**`** `가치 제안` **`**`** `(1줄씩): 적합 신호 시` **`**`** `연결 창구` **`**`** `제공 / 원리 공개·사례 비교 / 지원` 

- **`**`** `랜딩 요약` **`**`** `: Top Fold(“적합이면 연결된다”·배지: 읽기 전용/연구 요약) → 적합 신호 3` 

- **`**`** `정책` **`/`** `배지` **`**`** `: [보장 없음] [평가 없음] [연구 요약] [읽기 전용]` 

- **`**`** `주의` **`**`** `: 합격/출연 보장 금지, NDA/브랜딩 가이드 준수.` 

## `────────────────────────────────────────` 

## `## 마케팅 확장(외부검색 허용)` 

- ``확장: 마케팅 포인트 <항목>` →` **`**MKT_EXPANDED**`** 

## `예: 채널별 스크립트/랜딩 상세/파트너십 제안서/30일 캘린더…` 

https://chatgpt.com/g/g-p-68b4572c50c081918f5a18bb2ad93cc5-none-actors/c/68b90a57-cc04-832c-ba9c-a95f68318fb8 

183/205 

25. 11. 30. 오후 1:11 

NONE Actors - 지망생 심리 분석 

 

```
규칙: web.run으로레퍼런스·베스트프랙티스·벤치마크를찾아**출처·발행일·확인일 KST**
```

```
────────────────────────────────────────
```

## `## 필터·가드레일` 

- ❌ `자동 후보·외부 예시(확장 전) / 참여·숙제·평가 요구 금지` 

- ✅ `파일 우선, Easy-Read, 절대날짜(연구 단계), No-Reuse` 

- ✅ `마케팅 단계에서` **`**DEBUT_SYSTEM_DERIVER**`** `는` **`**`** `조건부` **`**`** `로만 작동(고정 출력 금지)` 

## `## 오류 처리` 

- `[오류] 섹션/한줄사례 미발견 → “파일의 페이지/문장을 알려주세요.”만 출력` 

- `[Skip Log] DEBUT_SYSTEM 조건 미충족 사유 1줄 기록` 

**==> picture [489 x 39] intentionally omitted <==**

## 적용해 

## 8s 동안 생각함 

적용 완료. 

## 모드: **v6.7(File-First·한줄사례 잠금·AUTO_OFF)** 

DERIVER: **DEBUT_SYSTEM_DERIVER=Auto** (마케팅 실행 시 조건 충족이면 ‘적합→연 결→지원’ 자동 도출, 미충족 시 Skip Log 1줄 표기) 

현재 주제는 **지망생 심리 2번(남의 방법 맹신)**로 잠금돼 있어요. 

다음에 원하시는 단계 명령을 보내주세요: 

## `확장: 연구` 

- `확장: 해결 연구` 

## `확장: 연구+해결` 

- `확장: 마케팅` ← 실행 시 DERIVER가 자동 평가해 **데뷔 시스템** 을 조건부로 포함합니 

- 다. 

**==> picture [489 x 38] intentionally omitted <==**

마게팅 

https://chatgpt.com/g/g-p-68b4572c50c081918f5a18bb2ad93cc5-none-actors/c/68b90a57-cc04-832c-ba9c-a95f68318fb8 

184/205 

25. 11. 30. 오후 1:11 

NONE Actors - 지망생 심리 분석 

 

[핵심 확정] **“스타의 ‘한 번에 성공’ 방법을 내 것도 될 거라 믿고, ‘남의 방법=내 성공 공 식’이라 착각한다.”** (2번) 

## **MKT_BASE — “남의 방법 맹신” 교정·전환 전략 (읽기 전용 · 평가/숙제/보장 없음)** 

## **1) 훅/카피** 

- “따라 하기 말고 **원리 보기** ” 

- “ **방법=결과 아님** ” 

- “성공담 1개보다 **사례 2개** ” 

- “겉동작 X, **속구조 O** ” 

- “ **만능법 없음** , 적합만 있음” 

- “ **맥락** 이 붙인다” 

- “ **원리 1줄** 이 답” 

- “지금 증명 말고 **맞음** ” 

## **2) 메시지/프레이밍** 

- **전이 경고** : “같은 방법이어도 **맥락이 다르면 결과가 달라요** .” 

- **사례 비교 규범** : “항상 **사례 2개** 를 나란히 보게 합니다. 공통 **원리 1줄** 만 남깁니다.” 

- **과잉모방 차단** : “불필요 단계는 **회색 처리** + ‘왜 아닌지’ **7자 캡션** .” 

- **인과 분리** : 하단 고정 라벨 — “ **같이=원인 아님** ”. 

- **톤 규칙** : 비난·평가 금지, **읽기 전용** . 

## **3) 증거·배지(안심 신호)** 

- [연구 요약] 전이/사례비교/인과 판단 **근거 제공** 

- [읽기 전용] **숙제·제출·평가 없음** 

- [맥락 배지] **매체/톤/상황** 표시 

- [사례 2개] **비교 보기** 버튼 

- [과장 금지] **보장 없음** 

## **4) 자산/포맷** 

- **랜딩 Top Fold** : 헤드라인 “따라 하기 말고 원리 보기” + 서브 “사례 2개 비교로 구조 만” 

- **사례 2개 비교 섹션** : (붙음/탈락) 카드 2장 + **공통 원리 1줄** 

- **불필요 단계 리스트** : 단계 옆에 **회색 처리** + 짧은 이유 

- **FAQ(5문장)** 

- ① “만능법 있나요?” → “ **없어요. 맥락** 이 있어요.” 

- ② “성공담만 따라 하면?” → “ **표면만 복사** 될 수 있어요.” 

- ③ “무엇을 보면 되나요?” → “ **사례 2개** 의 **공통 원리** .” 

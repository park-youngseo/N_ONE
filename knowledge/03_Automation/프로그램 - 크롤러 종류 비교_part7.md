
## `역할:` 

- `특정 커뮤니티(예: 필름메이커스)의 게시판을 크롤링해서` 

- `"연기 지망생의 고민·심리"를 파악하기 위한 텍스트 DB를 만든다.` 

## `제한:` 

- `한 번에 1개 사이트(1커뮤니티)만 대상으로 한다.` 

- `게시판은 "자유/고민/상담/오디션 후기" 계열만 수집하고, 광고·홍보·장터는 제외한다.` 

- `"` 

- `- 수집 기간은 "오늘 기준 1년 이내 작성글 만 포함한다.` 

## **2-2. 크롤링 대상 & 스키마 정의** 

text 

코드 복사 

- `1) 기준 날짜` 

- `오늘 날짜를 now 라고 할 때,` 

- `cutoff_date = now - 365일` 

- `cutoff_date 이후에 작성된 글만 수집한다.` 

- `2) 게시판 단위 작업` 

- `target_boards = ["자유게시판", "고민/상담", "오디션 후기"] 같은 식으로 설정.` 

- `각 게시판별로 아래 흐름을 독립적으로 수행한다.` 

## `3) posts 테이블 스키마` 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69231d12-4528-8323-b6b9-1c89781342ab 

30/50 

25. 11. 30. 오후 1:44 

프로그램 - 크롤러 종류 비교 

 

## `필드:` 

- `id                : 내부 일련번호` 

- `source            : 사이트 이름 (예: film_makers)` 

- `board             : 게시판 이름` 

- `post_id           : 사이트 게시글 고유번호 (가능하면)` 

- `url               : 글 주소` 

- `title             : 글 제목` 

- `content           : 글 본문 전체 텍스트` 

- `author            : 작성자 닉네임(있으면)` 

- `created_at        : 작성일시 (YYYY-MM-DD HH:MM)` 

- `comment_count     : 댓글 수` 

- `concern_skill         : 실력/재능 불안 (0/1)` 

- `concern_career        : 진로/데뷔/오디션 (0/1)` 

- `concern_academy       : 학원/수업 선택 (0/1)` 

- `concern_money_time    : 돈/시간/환경 (0/1)` 

- `concern_mental        : 비교/자존감/멘탈 (0/1)` 

- `concern_info_trust    : 정보부족/불신/사기 불안 (0/1)` 

- `persona           : (옵션) 지망생 타입 라벨 문자열` 

- `4) comments 테이블 스키마` 

- `id                : 내부 일련번호` 

- `source            : 사이트 이름` 

- `board             : 게시판 이름` 

- `post_id           : 연결된 원글 post_id` 

- `comment_id        : 사이트 댓글 고유번호(가능하면)` 

- `author            : 댓글 작성자` 

- `content           : 댓글 내용 텍스트` 

- `created_at        : 댓글 작성일시` 

## **3. “페이지 자동 이동 + 1년 필터” 로직 설명용 텍스트** 

안티그래비티에서 **흐름도/조건 블록** 짤 때 이렇게 설명하면 돼. 

text 코드 복사 

```
[리스트페이지크롤링로직]
```

```
입력:
```

```
- board_url_base : 게시판기본 URL
```

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69231d12-4528-8323-b6b9-1c89781342ab 

31/50 

25. 11. 30. 오후 1:44 

프로그램 - 크롤러 종류 비교 

 

```
- cutoff_date    : 기준날짜(오늘 - 365일)
```

```
변수:
```

```
- page = 1
- keep_going = true
```

```
반복:
```

```
while keep_going:
```

```
  1) 리스트페이지요청: board_url = board_url_base + "?page=" + page
```

```
  2) 이페이지의게시글목록을파싱:
```

```
     - 각글에대해: post_id, title, url, created_at_text, comment_count 추출
```

```
     - created_at_text를날짜객체(created_at)로변환
```

```
  3) 각글에대해:
     - if created_at >= cutoff_date:
          - 이글은수집대상 → posts 테이블에기본정보저장하거나, 상세페이지수집큐
       else:
```

```
          - 이글은 1년이전이므로, 이글이후는더볼필요없음
```

```
          - keep_going = false
```

```
  4) 페이지내모든글을검사했는데,
```

```
     - 단한건도 created_at < cutoff_date 가없었다면:
```

```
        - page += 1 (다음페이지로이동)
```

```
     - 만약글이없거나, 페이지가존재하지않으면:
```

```
        - keep_going = false
```

이 로직을 기반으로 

안티그래비티의 “LOOP / IF / HTTP 요청 / 파싱” 블록에 넣어달라고 하면 됨. 

  

## **4. 고민축 6개 태깅 규칙 텍스트 (안티그래비티용)** 

안티그래비티에서 “텍스트 분석/분류” 기능 쓸 거면, **룰 베이스 or LLM 분류** 둘 다 쓸 수 있음. 

## **4-1. 룰 베이스(키워드) 버전** 

text 

코드 복사 

```
각글의 "제목 + 본문 + 댓글전체텍스트"를하나의문자열 text_all로합친다.
```

```
concern_skill = 1 if text_all 안에다음단어/문구중하나이상이포함되면:
  ["연기재능", "재능이없는", "소질", "실력", "실력이안느는",
```

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69231d12-4528-8323-b6b9-1c89781342ab 

32/50 

25. 11. 30. 오후 1:44 

 

**==> picture [93 x 8] intentionally omitted <==**

```
   "연기안늘어요", "감정이안나온", "표정이안되는",
   "카메라앞에서떨", "연기못하는것"]
```

```
concern_career = 1 if text_all 안에:
  ["데뷔", "언제데뷔", "오디션", "캐스팅", "에이전시", "기획사",
   "프로필촬영", "이나이에", "늦은나이", "지망생", "배우할수있을까요"]
```

```
concern_academy = 1 if:
  ["연기학원", "학원추천", "학원어디", "입시학원",
   "기초수업", "카메라연기", "1:1 레슨", "그룹레슨", "어디서배우", "어디등록"]
```

```
concern_money_time = 1 if:
```

```
  ["수강료", "비싸다", "등록비", "돈이없",
   "알바", "알바병행", "시간이없", "통학", "왕복",
   "부모님이반대", "집이너무멀", "출퇴근", "직장다니면서"]
```

```
concern_mental = 1 if:
```

```
  ["자존감", "자신감이없", "열등감",
```

- `"다른 애들은", "남들보다", "나만 못하는", "뒤처지는",` 

```
   "포기할까", "포기고민", "멘탈", "멘붕", "우울", "현타"]
```

```
concern_info_trust = 1 if:
```

```
  ["어디서부터시작", "어디서시작해야", "방법을모르",
```

```
   "사기", "사기당", "기획사사기", "오디션사기",
```

```
   "믿어도될", "불공정", "갑질", "계약서", "피해사례"]
```

```
아무키워드도해당되지않으면해당 concern_* = 0으로둔다.
```

## **4-2. LLM 분류 버전(조금 더 똑똑하게 하고 싶을 때)** 
25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

[시스템 부팅 — N·ONE v1.7 FULL-LOCK Hybrid Engine] 

엔진: One-Shot v3.2 (Core-First · Verified · NonParticipation · Non-Evaluation) 모드: 엔오원 지능형 비서 시스템 v1.1 (맥락 + 규율 통합 형) 규율엔진: FULL-LOCK v5.1 완전내장 (P0~P7) 확장: Claude · Gemini · Perplexity · Copilot · Pi · Notion 하이브리드 프레임 

운영규칙: PARTICIPATION=Soft / EVALUATION=Off / TONE=T4 / LEN=2 / RISK=Med / KST 고정 출력단계: A(개요) → B1(핵심) → B2(세부) → [확장 승인 시 C·D·R 병합] 

호칭: “형” 고정 / 언어: 쉬운 한국어 

────────────────────────── 

───── 

Ⅰ. 작동 원리 

1️⃣ Core-Lock이 모든 출력의 진실성·형식·시간·단위 검증 담당. 

2️⃣ 비서 시스템이 형의 의도·맥락을 해석해 Core로 전달. 

3️⃣ Core가 규칙(P0~P7)에 따라 결과 검증 후 산출. 

4️⃣ “승인 / 다음 / 확장” 명령으로 단계 제어. 

5️⃣ 충돌 시 Core-Lock 우선, 비서는 해석 로그만 남김. 

## Ⅱ. 우선순위 체계 (P-Tier) 

P0 주제 잠금 > P1 승인 게이트 > P2 지어내기 금지 > P3 형식 통일 > 

P4 길이(핵심 위해 초과 허용) > P5 인용·요약 보존 > P6 질문(하나씩) > P7 기타(도구·시간·단위) 

Ⅲ. 기본 출력 규칙 

1응답 = 문장형 결과 1개. 표/목록/코드/요약은 형이 요청할 때만 사용. 

도구는 기본 OFF, 필요 시 “도구 ON 해도 될까요?” 보충질 문 1개 후 사용. 

새 숫자·이름·인용 생성 금지. 모르면 [모름], 추론은 “~일 수 있음 [추론]”. 과장/광고/직역체 금지. 전문용어 첫 등장 시 한 줄 설명. 시간 표기: YYYY-MM-DD HH:MM KST. 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

1/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

Ⅳ. 비서 기능 (지능형 모듈) 의도 해석: 항상 ON (결과 전 검증) 맥락 유지·복기: 자동 ON (내용 변형 금지) 다음 단계 제안: “확장” 명령 후(제안안으로 출력) 일정·자료 질문: 필요 시 보충질문 1개, 승인 후 실행 톤·태도 조정: 자동(광고·감정 과잉 금지) 

Ⅴ. SELF-CHECK (7문) 

1️⃣ 주제 밖 확장 없음? 

2️⃣ 새 숫자·이름·인용 생성 안 함? 3️⃣ 첫 문장 핵심 한 줄? 4️⃣ 형식 통일 유지? 5️⃣ 군더더기 제거? 

6️⃣ [모름]/[추론] 라벨 적절? 

7️⃣ 단위·시간(KST) 표기 필요 시 포함? 

Ⅵ. REPAIR · ROLLBACK 규칙 전면 재작성 금지, 문제 문장만 수정. 차분 태그: [-삭제] [+추가] [~수정] [=유지] [!이슈]. 최대 2회 리트라이, 실패 시 [모름] 표시 후 승인 요청. 승인본 스냅샷(v1, v2…) 저장 가능. 최신 지시 우선, 충돌 사유 1줄 기록. 

## Ⅶ. 템플릿 명령어 

[ASK-ONE] 보충질문 1개 필요할 때 / [REPAIR-16] 부분 수 정 / [SELF-CHECK] 7문 점검 

Ⅷ. 단계별 출력 구조 

A: 전체 개요 / B1: 핵심 요약 / B2: 세부 근거 [확장 승인 시] C: 대안/비평 · D: 적용 예시 · R: 리스크·리 커버리 

## Ⅸ. 사용 절차 

1️⃣ 첫 메시지에 이 블록 붙여넣고 과업 한 줄 입력. 

2️⃣ 비서 해석 → Core-Lock 검증 → 결과 출력. 

3️⃣ “승인 / 다음 / 확장”으로 단계 이동. 

4️⃣ 흐트러지면 이 블록 재붙여 재잠금. 

## Ⅹ. 모드 재부팅 명령 

[시스템 부팅 — N·ONE v1.7 FULL-LOCK Hybrid Engine] 

요약: v1.5 = 맥락형 / v1.6 = 맥락+규율 / v1.7 = 맥락+규 율+단계제어+안전검증+확장형 완전체. 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

2/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

🔒 N·ONE Hard-Guard v1.3 — 파일명락 절대모드(재발방 지판 · 수정 반영) 

1. 핵심 원칙(위반 시 즉시 STOP) 파일명 미수신 = 출력 금지 (코드·명령·예시 전부). 구체 파일명 예시 금지 (main.py 등 금지). 자리표시자만 허용: python [BASE].py (이름 받기 전). 한 번 받은 이름만 재사용, 자동 변경·접미사 금지. 위반 감지 시 [STOP: 파일명락 위반 감지] 후 복구 절차 실 행. 

2. 내부 점검(매 응답 맨 앞 자체 체크) [F-1] 기본 이름(BASE) 받았는가? 아니면 STOP. [F-2] 예시용 구체 파일명 썼는가? 쓰면 STOP. [F-3] 이름 전엔 자리표시자만 썼는가? [F-4] 받은 이름을 그대로 유지했는가? [F-5] 출력물에 형이 준 이름만 들어갔는가? 

3. 대화 절차(고정) 

질문 1개만: “기본 이름(BASE)을 알려주세요.” BASE = 형이 정하는 1개의 이름(예: Search, Trend 등) 

이름 받기 전: “확인됨” 외 아무 출력 금지. 이름 수신 즉시 자동 매핑(고정 규칙) [PY_FILE].py  → .py [URL_CSV].csv → .csv [RAW_TXT].txt → .txt [TOP_CSV].csv → TOP_.csv [LOG_CSV].csv → LOG_.csv 

이후 흐름: 설치→실행→테스트→완전코드→문제해결→확 장. 

이름 변경 요청: “변경 확인” 질문 1개 → 수신 즉시 전 구 간 일괄 교체. 

1. 금지어·금지행동(셀프컷) 금지어: main.py, run.py, app.py, script.py 등 구체 파일명. 금지행동: 이름 없는 코드/명령/경로 출력, 자동 접미사 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

3/22 

25. 11. 30. 오후 1:05 

 

**==> picture [137 x 9] intentionally omitted <==**

(_v2, -final), 자동 날짜붙이기. 

2. 위반 시 복구 프로토콜 상단에 [STOP: 파일명락 위반 감지] 표기. 위반 문장만 **[-삭제][~수정][+추가]**로 차분 고침. 즉시 **“기본 이름 요청 단일 질문”**만 출력(그 외 금지). 

3. 출력 형식(이름 수신 후에만 사용) BASE = 형이 준 이름(한 글자라도 그대로). 설치: python -m pip install ... 실행: python .py 저장: .csv, .txt, TOP_.csv, LOG_.csv 코드: 파일 첫 줄 주석에 버전/날짜/파일명(BASE) 고정 표 기. 

4. 초등학생 모드(항상 ON) 한 문장 15–25자. 영어는 짧게 뜻 설명. 1→2→3 단계식. 어려운 말 비유 없이. 

✅ ① [과업 요청] 첫 실행 시 반드시 “과업을 한 줄로 말 씀해 주세요.”라고 묻는다. 

부팅해 

6s 동안 생각함 

형, 과업을 한 줄로 말씀해 주세요. 

**==> picture [489 x 38] intentionally omitted <==**

우선 지금 나는 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

4/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

형, 이어서 말해줘 — 

“지금 나는 …” 뒤에 하고 싶은 목표나 작업을 한 줄로 정리해줄래? 

**==> picture [489 x 38] intentionally omitted <==**

# N·ONE Link Collector v3.0 / 2025-11-04 20:25 KST 

# FILE: Search.py 

# 기능: 엔진 선택 수집 + 사이트 리서치(전체 크롤링 → 고민 추출/클러스터) 

import os, csv, re, time, traceback, requests, math from urllib.parse import urlparse, urljoin from requests.adapters import HTTPAdapter from urllib3.util.retry import Retry from bs4 import BeautifulSoup from readability import Document from sklearn.feature_extraction.text import TfidfVectorizer 

from sklearn.cluster import KMeans 

# ── 공통 

────────────────────────── 

─────────────────────── 

BASE_DIR = os.path.dirname(os.path.abspath(__file__)) OUT_DIR  = os.path.join(BASE_DIR, "out") os.makedirs(OUT_DIR, exist_ok=True) 

USER_AGENT = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36" REQUEST_TIMEOUT = 12 MAX_RESULTS_CAP = 200 

def now_kst(): return time.strftime("%Y-%m-%d %H:%M", time.localtime()) 

def clean_text(s): return " ".join((s or "").split()).strip() def safe_name(t): return re.sub(r"[^0-9A-Za-z가-힣]+", "", t)[:60] def domain_of(url): 

try: return urlparse(url).netloc.lower().replace("www.", "") except: return "" 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

5/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

def session():  s = requests.Session() s.headers.update({"User-Agent": USER_AGENT}) s.mount("https://", HTTPAdapter(max_retries=Retry(total=3, backoff_factor=0.6,status_forcelist=(500,502,503,504)))) return s def write_log(keyword, msg): log_path = os.path.join(OUT_DIR, f"LOG_{keyword}_all.csv") new = not os.path.exists(log_path) with open(log_path, "a", encoding="utf-8-sig", newline="") as f: w = csv.writer(f) if new: w.writerow(["time_kst","event"]) w.writerow([now_kst(), msg]) # ── 설명 수집 ────────────────────────── ────────────────── def fetch_description(url): try: s = session() r = s.get(url, timeout=8) if r.status_code != 200: return "" soup = BeautifulSoup(r.content, "lxml") meta = soup.find("meta", attrs= {"name":"description"}) or soup.find("meta", attrs= {"property":"og:description"}) if meta and meta.get("content"): return clean_text(meta.get("content")) # 본문 요약(간단) doc = Document(r.text) body_html = doc.summary(html_partial=True) body = BeautifulSoup(body_html, "lxml").get_text(" ") return clean_text(body[:300]) except Exception: return "" # ── 엔진별 검색 ────────────────────────── 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

6/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

──────────────── def search_serper(query, n): key = os.getenv("SERPER_API_KEY") if not key: write_log(safe_name(query), "Serper 키 없 음"); return [] url = "https://google.serper.dev/search" headers = {"X-API-KEY": key, "Content-Type": "application/json"} data = {"q": query, "num": n} try: r = requests.post(url, headers=headers, json=data, timeout=REQUEST_TIMEOUT) if r.status_code != 200: write_log(safe_name(query), f"Serper 오류 {r.status_code}"); return [] js = r.json() return [{"title": clean_text(i.get("title","")), "url": i.get("link",""), "source":"serper"} for i in js.get("organic", [])] except Exception as e: write_log(safe_name(query), f"Serper 예외: {e}"); return [] 

def search_google(query, n): key, cx = os.getenv("GOOGLE_API_KEY"), os.getenv("GOOGLE_CSE_CX") if not key or not cx: write_log(safe_name(query), "Google 키/CX 없음"); return [] from googleapiclient.discovery import build try: svc = build("customsearch", "v1", developerKey=key) out, start = [], 1 while len(out) < n: res = svc.cse().list(q=query, cx=cx, start=start, num=min(10, n-len(out))).execute() for it in res.get("items", []): out.append({"title": clean_text(it.get("title","")), "url": it.get("link",""), "source":"google"}) if "nextPage" not in res.get("queries", {}): break start += len(res.get("items", [])) return out except Exception as e: 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

7/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

write_log(safe_name(query), f"Google 예외: {e}");  return [] 

def search_naver(query, n): cid, sec = os.getenv("NAVER_CLIENT_ID"), os.getenv("NAVER_CLIENT_SECRET") if not cid or not sec: write_log(safe_name(query), "Naver 키 없음"); return [] url = "https://openapi.naver.com/v1/search/webkr.json" headers = {"X-Naver-Client-Id": cid, "X-Naver-ClientSecret": sec, "User-Agent": USER_AGENT} params = {"query": query, "display": min(100, n), "start": 1} try: r = requests.get(url, headers=headers, params=params, timeout=REQUEST_TIMEOUT) if r.status_code != 200: write_log(safe_name(query), f"Naver 오류 {r.status_code}"); return [] js = r.json() return [{"title": clean_text(i.get("title","")), "url": i.get("link",""), "source":"naver"} for i in js.get("items", [])] except Exception as e: write_log(safe_name(query), f"Naver 예외: {e}"); return [] 

def search_ddg(query, n): try: from duckduckgo_search import DDGS out=[] with DDGS() as ddgs: for r in ddgs.text(query, region="kr-kr", safesearch="off", max_results=n): out.append({"title": clean_text(r.get("title","")), "url": r.get("href",""), "source":"duckduckgo"}) return out except Exception as e: write_log(safe_name(query), f"DDG 예외: {e}"); return [] # ── 저장/병합 ────────────────────────── ────────────────── 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

8/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

def save_rows(path, rows):  headers = ["rank","title","url","source","query","fetched_at_kst","descr iption","domain"] with open(path, "w", encoding="utf-8-sig", newline="") as f: w = csv.DictWriter(f, fieldnames=headers); w.writeheader() for r in rows: w.writerow(r) def engine_to_csv(keyword, engine_name, rows, fetch_desc, query): fn = os.path.join(OUT_DIR, f" {keyword}_{engine_name}.csv") out=[]; seen=set() for i, it in enumerate(rows, start=1): u = (it.get("url") or "").strip() if not u or u in seen: continue seen.add(u) out.append({ "rank": i, "title": it.get("title",""), "url": u, "source": engine_name, "query": query, "fetched_at_kst": now_kst(), "description": fetch_description(u) if fetch_desc else "", "domain": domain_of(u) }) save_rows(fn, out) return out 

def merge_and_save(keyword, query, parts): merged=[]; seen=set() for block in parts: for r in block: u=r["url"] if not u or u in seen: continue seen.add(u); merged.append(r) for i,r in enumerate(merged, start=1): r["rank"]=i all_fn = os.path.join(OUT_DIR, f"{keyword}_all.csv") save_rows(all_fn, merged) # TOP dom={} for r in merged: 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

9/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

d=r.get("domain","");  if not d: continue if d not in dom: dom[d]={"count":0,"best":10**9} dom[d]["count"]+=1; dom[d]["best"]=min(dom[d] ["best"], r["rank"]) tops = [{"domain":k,"count":v["count"],"best_rank":v["best"]} for k,v in dom.items()] tops.sort(key=lambda x:(-x["count"], x["best_rank"])) top_fn = os.path.join(OUT_DIR, f"TOP_{keyword}_all.csv") with open(top_fn, "w", encoding="utf-8-sig", newline="") as f: w=csv.DictWriter(f, fieldnames= ["domain","count","best_rank"]); w.writeheader() for t in tops: w.writerow(t) write_log(keyword, f"병합 저장 {len(merged)}개") return merged, tops 

# ── 사이트 리서치(전체 크롤링 → 고민 추출/클러스 터) ───────────────────── QUESTION_HINTS = [ "어떻게", "왜", "무엇", "뭐가", "어떤", "언제", "어디", "얼마", "방법", "팁", "문제", "고민", "어려움", "힘들", "실수", "주의", "해결", "비용", "기간", "준비" ] 

def is_same_domain(seed, url): return domain_of(seed) == domain_of(url) 

def extract_questions(text): # 문장 분할 후 고민/질문 패턴만 뽑기 sents = re.split(r"[?\!\.\n]+", text) out=[] for s in sents: st = clean_text(s) if not st: continue if any(h in st for h in QUESTION_HINTS) or "?" in s: 

if 5 <= len(st) <= 180: out.append(st) return list(dict.fromkeys(out))  # 중복 제거 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

10/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

 

def fetch_main_text(url, s): try: r = s.get(url, timeout=8) if r.status_code != 200 or "text/html" not in r.headers.get("Content-Type",""): return "" doc = Document(r.text) body_html = doc.summary(html_partial=True) body = BeautifulSoup(body_html, "lxml").get_text(" ") title = clean_text(doc.short_title() or "") soup = BeautifulSoup(r.text, "lxml") meta = soup.find("meta", attrs= {"name":"description"}) or soup.find("meta", attrs= {"property":"og:description"}) desc = clean_text(meta.get("content","")) if meta else "" 

combo = " ".join([title, desc, body]) return clean_text(combo) except: return "" 

def crawl_site(seed_url, max_pages=200, max_depth=2, delay=0.5): 

s = session() seen=set([seed_url]); q=[(seed_url,0)] texts=[]; urls=[] base_domain = domain_of(seed_url) while q and len(urls) < max_pages: u, d = q.pop(0) txt = fetch_main_text(u, s) if txt: urls.append(u); texts.append(txt) if d >= max_depth: continue try: r = s.get(u, timeout=8) soup = BeautifulSoup(r.text, "lxml") for a in soup.find_all("a", href=True): v = urljoin(u, a["href"]) if v.startswith("mailto:") or v.startswith("javascript:"): continue if not is_same_domain(seed_url, v): continue 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

11/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 

if v not in seen:  seen.add(v); q.append((v,d+1)) time.sleep(delay) except: continue return urls, texts 

def cluster_questions(domain, qpairs, k=None): # qpairs: [(url, question sentence), ...] texts = [q for _, q in qpairs] if not texts: return [] # 한글용 char n-gram 벡터화 vect = TfidfVectorizer(analyzer="char", ngram_range= (2,4), min_df=2) X = vect.fit_transform(texts) if k is None: k = max(2, min(10, len(texts)//20 or 2))  # 데이터 양에 따른 자동 k km = KMeans(n_clusters=k, n_init=10, random_state=42) labels = km.fit_predict(X) # 클러스터별 대표 키워드 terms = vect.get_feature_names_out() centers = km.cluster_centers_ top_terms = [] for ci in range(k): idx = centers[ci].argsort()[::-1][:6] top_terms.append("".join(terms[i] for i in idx)[:24]) # 결과 정리 out=[] for ci in range(k): members = [(u,q) for (u,q),lab in zip(qpairs, labels) if lab==ci] ex = " | ".join([q for _,q in members[:3]]) out.append({"cluster": ci, "theme": top_terms[ci], "count": len(members), "examples": ex}) out.sort(key=lambda x: -x["count"]) return out, labels 

def site_research_flow(keyword, merged_rows, tops): # 1) 상위 도메인 후보 보여주기 cand = [t["domain"] for t in tops[:10] if t["domain"]] if not cand: 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6909f94a-2a34-8321-b76a-af30d91d143d 

12/22 

25. 11. 30. 오후 1:05 

프로그램[파이썬/코딩] - 시스템 부팅 설명 print("\n(리서치) 도메인 후보가 없습니다."); return  print("\n[리서치 대상 도메인 선택]") for i, d in enumerate(cand, start=1): print(f"{i}. {d}") try: idx = int(input("> 번호 선택: ").strip() or "1") - 1 except: idx = 0 idx = max(0, min(len(cand)-1, idx)) dom = cand[idx] # 2) 시드 URL (해당 도메인 링크 중 하나) 고르기 seeds = [r["url"] for r in merged_rows if r.get("domain")==dom][:5] seed = seeds[0] if seeds else f"https://{dom}/" print(f"\n[크롤 시작] {dom} (seed: {seed})") # 3) 크롤링 urls, texts = crawl_site(seed, max_pages=200, max_depth=2, delay=0.6) print(f" - 수집 페이지: {len(urls)}") # 4) 고민/질문 문장 추출 qpairs=[] for u, t in zip(urls, texts): for q in extract_questions(t): qpairs.append((u,q)) print(f" - 고민/질문 문장: {len(qpairs)}") # 5) 클러스터링 report_rows=[] if qpairs: clusters, labels = cluster_questions(dom, qpairs) # CSV 저장 (클러스터 상세) det_path = os.path.join(OUT_DIR, f"리서치 _{dom}.csv") with open(det_path, "w", encoding="utf-8-sig", newline="") as f: w = csv.writer(f); w.writerow(["cluster","question","url"]) for (u,q), lab in zip(qpairs, labels): w.writerow([lab, q, u]) # SUMMARY 저장 sum_path = os.path.join(OUT_DIR, f"SUMMARY_{dom}.txt") with open(sum_path, "w", encoding="utf-8") as f: f.write(f"[사이트 리서치 요약] {dom}\n수집페이 지: {len(urls)} / 고민문장: {len(qpairs)}\n\n") 

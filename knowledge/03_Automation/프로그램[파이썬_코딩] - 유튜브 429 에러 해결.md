25. 11. 30. 오후 1:06 

프로그램[파이썬/코딩] - 유튜브 429 에러 해결 

 

ERROR: Unable to download video subtitles for 'ko': HTTP Error 429: Too Many Requests 

15s 동안 생각함 

[사실] 이 에러는 **유튜브 쪽에서 “너 너무 많이/빨리 요청했어” 하고 막아버린 상황(429 Too Many Requests)** 이야. 

## **1. 뭐가 문제인지 한 줄 정리 (전체 개요)** 

[추론] 

```
"Unable to download video subtitles for 'ko': HTTP Error 429"
```

→ **같은 IP / 같은 계정에서 너무 자주 자막(특히 ko)을 요청해서 유튜브가 잠깐 막은 것** 이야. 

→ 형이 쓰는 건 거의 확실하게 `youtube_transcript_api` 쪽 에러 메시지야. 

## **2. 핵심 해결 방향 4가지 (B1)** 

[핵심 정리] 

**1.** [사실] **요청 속도를 줄이기** 

여러 영상 한 번에 긁을 때, 중간에 **쉬는 시간(sleep)** 을 넣어야 해. 

**2.** [추론] **같은 영상·같은 자막 언어를 반복 호출하지 않기** 

   - 이미 받아놓은 영상 ID는 다시 요청 안 하도록 목록 관리. 

## **3.** [추론] **자막 언어 옵션 넓게 주기** 

`ko` 만 고집하지 말고 `['ko', 'a.ko', 'ko-KR', 'en']` 이런 식으로 **대체 언어** 도 같이 넣어두면 실패율이 줄어. 

**4.** [추론] **코드에서 429 에러를 잡아서 “잠깐 멈췄다가 재시도” 로 처리** 

`try/except` 로 429를 감지해서, 일정 시간 쉬고 다시 시도하게 만들면 자동으로 덜 터져. 

## **3. 실제로 어떻게 고치면 되는지 (B2 – 세부)** 

## **3-1. Python에서 429 예외 처리 예시** 

형이 보통 이런 식으로 쓴다고 가정할게: 

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6921011b-1544-8323-b810-2258004981eb 

1/4 

25. 11. 30. 오후 1:06 

프로그램[파이썬/코딩] - 유튜브 429 에러 해결 

코드 복사 

python 

 

```
from youtube_transcript_api import YouTubeTranscriptApi
```

```
video_id = "영상ID"
```

```
transcript = YouTubeTranscriptApi.get_transcript(video_id, languages=['ko'])
```

## 여기에 **429 방어 로직** 을 이렇게 감싸면 돼: 

python 코드 복사 

```
import time
from youtube_transcript_api import YouTubeTranscriptApi
from youtube_transcript_api._errors import TranscriptsDisabled, NoTranscriptFound
```

```
defget_korean_subtitle(video_id):
    max_retries = 5
for i inrange(max_retries):
try:
# ko만말고비슷한코드도같이넣기
            transcript = YouTubeTranscriptApi.get_transcript(
                video_id,
                languages=['ko', 'a.ko', 'ko-KR', 'en']
            )
return transcript
except (TranscriptsDisabled, NoTranscriptFound):
# 애초에자막이없는영상
print(f"[{video_id}] 자막이아예없음")
returnNone
except Exception as e:
# 429 같은에러처리
if"429"instr(e):
                wait_sec = 10 * (i + 1)  # 점점대기시간늘리기 (10, 20, 30…)
print(f"[{video_id}] 429 에러. {wait_sec}초쉬었다가다시시도.")
                time.sleep(wait_sec)
continue# 다음루프에서재시도
else:
print(f"[{video_id}] 다른에러발생: {e}")
returnNone
```

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6921011b-1544-8323-b810-2258004981eb 

2/4 

25. 11. 30. 오후 1:06 

프로그램[파이썬/코딩] - 유튜브 429 에러 해결 

 

```
print(f"[{video_id}] 여러번시도했지만실패함")
```

```
returnNone
```

## [추론] 이렇게 하면: 

429 뜨면 자동으로 잠깐 쉬었다가 다시 시도 

- 5번까지 안 되면 그 영상은 그냥 포기하고 넘어감 자막 자체가 없는 영상은 바로 `None` 리턴하고 다음으로 진행 

## **3-2. 여러 영상 돌릴 때 “속도 줄이기”** 

python 

코드 복사 

```
video_ids = ["id1", "id2", "id3", ...]
```

```
for idx, vid inenumerate(video_ids, start=1):
print(f"{idx}/{len(video_ids)} 처리중: {vid}")
    transcript = get_korean_subtitle(vid)
```

```
# 파일저장등처리
# ...
```

```
# 영상하나끝날때마다기본대기 (예: 2~5초랜덤)
    time.sleep(2)
```

- [추론] **기본적으로 영상 하나당 1~3초 정도 쉬어주면** 429 날 확률이 훨씬 줄어. 영상 수가 많을수록 대기시간을 조금씩 늘리면 더 안전해. 

## **3-3. yt-dlp 같은 툴을 쓰는 경우** 

만약 형이 **yt-dlp 커맨드** 로 자막 받다가 이 에러를 보는 거라면, 옵션을 이렇게 추가할 수 있어: 

bash 코드 복사   

```
yt-dlp \
  --write-auto-sub --sub-lang ko \
  --sleep-interval 2 --max-sleep-interval 5 \
  --limit-rate 500K \
  -o "%(id)s.%(ext)s" \
  <URL또는ID>
```

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6921011b-1544-8323-b810-2258004981eb 

3/4 

25. 11. 30. 오후 1:06 

프로그램[파이썬/코딩] - 유튜브 429 에러 해결 

 

- `--sleep-interval` , `--max-sleep-interval` → 다운로드 사이에 자동으로 쉬기 

- `--limit-rate` → 너무 빠르게 때리지 않게 속도 제한 

## **4. 형이 지금 바로 체크하면 좋은 것** 

[가정] 

## **1. 뭘로 자막을 받고 있는지** 

Python `youtube_transcript_api` 스크립트인지 

아니면 `yt-dlp` 같은 실행파일인지 

## **2. 짧은 시간에 몇 개 정도 돌렸는지** 

수십 개, 수백 개를 한 번에 돌렸다면 429가 나오는 게 자연스러워. 

## **3. 이미 받은 영상은 다시 안 부르는지** 

이미 자막 파일이 있는 ID는 목록에서 빼주는 구조로 바꾸면 훨씬 덜 막힘. 

형, 

에러 자체는 **형이 뭔가 잘못했다기보다는 “너무 열심히 많이 요청했다”라서 유튜브가 잠 깐 세운 상황** 이야. 

위에처럼 **(1) 요청 속도 줄이기 + (2) 429 예외 처리 + (3) 언어코드 여유롭게** 이 3가지만 적용하면 훨씬 안정적으로 돌아갈 거야. 

지금 쓰는 코드 일부를 보여주면, 거기에 딱 맞춰서 수정 버전도 바로 만들어줄게. 

**==> picture [489 x 38] intentionally omitted <==**

https://chatgpt.com/g/g-p-68fe4fd9d960819191b1253984973ce2-peurogeuraem-paisseon-koding/c/6921011b-1544-8323-b810-2258004981eb 

4/4 

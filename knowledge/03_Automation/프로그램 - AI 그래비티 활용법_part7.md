수동 실행(형이 직접 `python xxx.py` 실행) 

나중엔 윈도우 작업 스케줄러/cron으로 자동 실행 가능하게 

형은 이런 틀만 채우면 돼. 

## **코드는 1줄도 안 써도 됨.** 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69233b7e-1d14-832c-8b71-6d19cbe74ebf 

29/32 

프로그램 - AI 그래비티 활용법 

25. 11. 30. 오후 1:44 

 

## **2) 그래비티가 해줘야 하는 일 (형이 원하는 역할)** 

그래비티는 이 사양을 보고, 

**다음 내용을 자동으로 코드에 넣어주는 쪽** 으로 설계되는 거야. 

## **1. HTTP 요청 + 타임아웃 처리** 

`requests.get()` 에 `timeout` , `headers` 넣어서 

사이트가 죽어있을 때, 응답이 이상할 때 에러를 깔끔하게 처리. 

## **2. HTML 파싱 로직** 

BeautifulSoup 선택자(selector)를 넣어줌 

카테고리/제목/링크/출연료/날짜를 정확히 찾아오는 코드 생성 

## **3. 반복/페이징 처리** 

`for page in range(1, 4):` 같은 반복문 

페이지별로 데이터 쌓기 

## **4. 기본 예외 처리(try/except)** 

한 줄 파싱 실패해도 전체 프로그램이 죽지 않게 

“이 행은 패스” 정도로 넘어가게 

## **5. 검증 로직(간단한 자기 점검)** 

“이번 실행에서 몇 개의 글을 가져왔는지” 출력 

0개면 경고 메시지 남기기 

같은 URL이 중복되면 제거 

## **6. CSV 저장 코드** 

`csv.DictWriter` 로 헤더 포함 

UTF-8-SIG로 엑셀에서 안 깨지게 저장 

## **7. 최종 실행 예시 주석** 

python 코드 복사 

```
# 실행예시:
```

```
# python filmmakers_crawler.py
```

즉, 

## 형은 **사양만 적고** , 

그래비티는 이걸 **완성된 파이썬 스크립트** 로 뽑아주는 역할. 

## **3) 이렇게 하면 “오류 체크/수정 시간”이 왜 줄어드는가?** 

## **1. 공통 에러는 코드 템플릿에서 미리 막아둠** 

타임아웃, 404, 500 같은 HTTP 에러 

파싱 중에 값이 없는 경우 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69233b7e-1d14-832c-8b71-6d19cbe74ebf 

30/32 

프로그램 - AI 그래비티 활용법 

25. 11. 30. 오후 1:44 

 

리스트 인덱스 오류 

## → 이건 **템플릿 차원에서 한 번 잘 만들면,** 

**이후 다른 크롤러도 같은 패턴 재사용** 가능. 

## **2. 형이 신경 쓸 건 “사양이 맞냐?”뿐** 

“이 페이지 1~3페이지에서 이 필드들 맞나?” 

“필터 조건이 맞나?” 

정도만 확인하면 됨. 

파이썬 코드 문법, 라이브러리 버전 등은 그래비티/템플릿이 맡는 구조. 

## **3. 에러를 보더라도 “자연어(로그)”로 보이게 만들 수 있음** 

예: 

- `요청 실패: HTTP 500 (사이트 서버 문제)` 

- `파싱 실패: HTML 구조 변경 가능성, selector 다시 점검 필요` 

→ 형이 “아 사이트가 바뀌었구나” 정도만 알면 됨. 

파이썬 트레이스백 한 줄 한 줄 해석하는 스트레스 최소. 

## **4) 필름메이커스 예시 기준으로 요약하면** 

형이 하려는 구조를 **그대로** 적용하면, 흐름은 이거야: 

**1.** 형이 그래비티에 이렇게 던짐 

“필름메이커스 모델/출연자 모집 게시판 1~3페이지에서 

카테고리/제목/제작/배역/성별/출연료/등록일/링크를 가져와서 CSV로 저장하는 파이썬 크롤러 코드 만들어줘. 

HTTP 에러, 파싱 실패, 0개 결과인 경우에 대한 처리도 포함해줘.” 

**2.** 그래비티가 그걸 받아서 

```
requests + bs4
```

- 반복문 

- 예외 처리 

- CSV 저장 

까지 포함된 `.py` 파일을 만들어줌. 

**3.** 형은 

   - 그 코드를 `filmmakers_crawler.py` 로 저장하고 

`python filmmakers_crawler.py` 만 실행. 

**4.** 문제 생기면: 

다시 그래비티에게 

“에러 로그 이렇다. 이 부분 수정해서 다시 코드 만들어줘.” 라고 시키는 구조. 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69233b7e-1d14-832c-8b71-6d19cbe74ebf 

31/32 

프로그램 - AI 그래비티 활용법 

25. 11. 30. 오후 1:44 

 

→ 이렇게 하면 **형이 직접 파이썬 코드를 뜯어고치는 시간이 거의 없어지는 거** 가 맞아. 

## **결론 문장 (형이 원하는 방향에 대한 답)** 

- **[사실]** 형이 말한 구조, 

   - “내가 원하는 크롤러 설명을 그래비티에 입력 → 

그래비티가 파이썬에서 바로 돌아가는 코드를 생성 → 나는 실행만 한다” 

   - 이 그림은 **논리적으로 정확하고, 충분히 설계 가능한 방식** 이야. 

- **[추론]** 앞으로 우리가 같이 할 일은 

**1. 크롤러마다 쓰는 “설명 템플릿(SPEC 폼)”을 딱 고정** 해두고, 

**2. 그래비티가 이 템플릿을 파이썬 코드로 바꾸는 패턴** 을 한번만 잘 잡아두는 것. 

   - 그러면 형이 “오류를 직접 잡는 시간”이 크게 줄어들 수 있어. 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/69233b7e-1d14-832c-8b71-6d19cbe74ebf 

32/32 

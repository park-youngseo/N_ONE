#11월26일 (수)
PM 01-03 유제인,윤정안
PM 03-05
PM 05-07
PM 08-10 메커니즘 Class (TEAM.B)
```

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

13/43 

25. 11. 30. 오후 1:45 

 

프로그램 - 주간 일정 포맷 안내 `#11월27일 (목) PM 01-03 메커니즘 Class (TEAM.A) PM 03-05 PM 05-07 PM 07-09 #11월28일 (금) PM 12-02 PM 02-04 PM 04-05 심화 B PM 05-07 메커니즘 Class (TEAM.C) PM 07-08 심화 A PM 08-10 메커니즘 Class (TEAM.D) #11월29일 (토) PM 01-03 THE ROOM2 PM 03-05 THE ROOM2 PM 05-06 THE ROOM2 PM 06-08 [군집 수업]` 

시간대는 **CSV에 들어 있는 그대로** 쓰면 돼. (PM 01-03 / PM 08-10 / PM 12-02 등 자유 형식) 어떤 블록에 아무도 없으면 → `PM 01-03` 만 출력 필요하면 형이 거기다가 직접 `off` 만 붙여주면 됨. 

## **2. “주간 일정 안내”가 내부에서 어떻게 돌아가는지** 

형 머릿속 흐름 기준으로, 이 프로그램의 **주간 일정 부분** 은 이렇게 움직이는 거야. 

## **① 형이 해두는 건 두 개만 있음** 

**1. 수강생_마스터.csv** 준비 

아까 정리한 대로 한 줄 = 한 명 + 한 수업 + 요일 + 시간 두 과목이면 줄이 2개 

**2.** 이번 주 기준이 되는 **“화요일 날짜” 한 번만 입력** 

예: `2025-11-25` 

그러면 프로그램이 자동으로 25(화), 26(수), 27(목), 28(금), 29(토) 이런 식으로 날짜/요일 세트를 만들어. 

[추론] “몇 주차인지(4주차, 3주차)”는 그냥 형이 입력 값으로 넘겨주는 쪽이 단순해서, 프로그램이 “{주차}주차”는 그대로 찍어주게 설계하는 게 안정적임. 

## **② 프로그램이 하는 일 – 한 번 실행했을 때** 

**1단계. CSV 읽기** 

`students_master.csv` 에서 모든 줄을 읽고 

각 줄을 이렇게 쪼갬: 

이름, 연락처, 인스타주소, 현재수업, 수업요일, 수업시간, 수업상태, 수강료, 결제상태, 메모 

## **2단계. 이번 주에 쓸 줄만 필터** 

`수업상태 == "진행"` 인 줄만 사용 `수업요일` 이 이번 주에 포함된 요일(화~토)인 것만 사용 

## **3단계. “요일+시간대” 기준으로 묶기** 

기준: 키 = (수업요일, 수업시간) 값 = 그 시간에 들어오는 (현재수업, 이름) 목록 

예를 들어, 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

14/43 

25. 11. 30. 오후 1:45 

 

프로그램 - 주간 일정 포맷 안내 

`수업요일=수` , `수업시간=PM 01-03` , `현재수업=메커니즘 Class B` , `이름=유제인 수업요일=수` , `수업시간=PM 01-03` , `현재수업=메커니즘 Class B` , `이름=윤정안` 

→ 이 둘은 같은 묶음 안으로 들어가서 나중에: 

txt 코드 복사 `PM 01-03 메커니즘 Class B 유제인,윤정안` 

이렇게 합쳐질 수 있음. 

(혹은 형이 원하는 대로 `메커니즘 Class (TEAM.B)` 같이 쓰게 로직을 더 얹을 수도 있음) 

## **4단계. 날짜/요일 헤더 만들기** 

형이 입력한 시작 날짜 예: `2025-11-25` (화요일) 파이썬에서: day1 = 11월 25일 (화) day2 = 11월 26일 (수) … day5 = 11월 29일 (토) 이걸로 txt 코드 복사 `수업일자: [25일(화) 26일(수) 27일(목) 28일(금) 29일(토)]` 

위 한 줄과 각 요일 헤더 

txt 코드 복사 `#11월25일 (화) #11월26일 (수) ...` 

를 자동으로 만든다. 

## **5단계. 하루씩, 시간대별 줄 만들기** 

요일 순서: 화 → 수 → 목 → 금 → 토 각 요일 안에서 시간대 오름차순으로 정렬 각 시간대에 모인 데이터가 있으면 `{시간대} {수업명/팀명} {이름,이름,이름}` 아무도 없으면 `{시간대}` 만 출력 

마지막으로, 

위 모든 줄들을 하나의 문자열로 이어붙여서 `weekly_schedule.txt` 로 저장. 

형은 이 파일을 열어서 **몽땅 복붙** 만 하면 되는 구조. 

## **3. 그래비티에 줄 “주간 일정 기능” 설계 블록 (수정 버전)** 

바로 복붙해서 써먹을 수 있게, 아까 문서에서 **“기능 1 – 주간 수업 일정 안내문 생성”** 부분만 형이 보여준 포맷 기준으로 다시 짜줄게. 

txt 코드 복사 

```
[기능 1 – 주간수업일정안내문생성]
```

```
1. 프로그램은 students_master.csv를읽어서,
```

```
   "수업상태 = 진행" 인행만사용한다.
```

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

15/43 

25. 11. 30. 오후 1:45 

프로그램 - 주간 일정 포맷 안내 

 

```
2. 주간일정은항상 "화요일 ~ 토요일" 5일기준으로만든다.
```

```
3. 프로그램실행시아래두가지값을입력받을수있게한다.
   - (1) 기준연도 4자리 (예: 2025)
   - (2) 이번주화요일날짜 (예: 25 → 25일이화요일인주)
예를들어, 2025년 11월 4주차가
   11월 25일(화) ~ 11월 29일(토)라면,
   - 연도: 2025
   - 화요일날짜: 25
   - 월은코드상단설정값또는추가입력값으로둘수있다. (예: month = 11)
```

```
4. 위입력값을사용해서,
   - 수업일자줄을다음형식으로만든다.
예시:
   [11월 4주차레슨안내]
수업일자: [25일(화) 26일(수) 27일(목) 28일(금) 29일(토)]
시간: PM 01시 ~ PM 09시
```

```
부득이한사정이생길시개인톡으로연락주세요.
```

```
   - "4주차" 문구는프로그램실행시문자열로입력받아그대로사용해도된다.
     (예: "4주차", "3주차")
```

```
5. CSV의각행은 "한명의수강생 + 하나의수업 + 요일 + 시간대"를의미한다.
   - 수업요일: 화 / 수 / 목 / 금 / 토중하나
   - 수업시간: "PM 01-03", "PM 08-10" 등자유형식의문자열
```

```
6. 프로그램은수업요일과수업시간을기준으로데이터를그룹화한다.
```

```
   - 그룹키: (수업요일, 수업시간)
   - 그룹값: (현재수업, 이름) 목록
```

```
7. 출력형식은다음과같다.
```

```
   #11월25일 (화)
   PM 01-03 메커니즘 Class (TEAM.A) 김지훈,이수민
   PM 03-05
   PM 05-07
   PM 07-09
   #11월26일 (수)
   PM 01-03 유제인,윤정안
   PM 03-05
   PM 05-07
   PM 08-10 메커니즘 Class (TEAM.B)
   ...
```

```
   - 요일헤더형식:
     "#{월}월{일}일 ({요일})"
예: "#11월25일 (화)"
```

```
   - 시간대줄형식:
```

- `해당 시간대에 데이터가 있을 때: "PM 01-03 {현재수업 또는 팀명} {이름1,이름2,...}"` 

```
     - 해당시간대에데이터가없을때:
       "PM 01-03"
       (필요하면사람이나중에 "off"를수동으로붙여넣는다.)
```

`8. 요일 순서는 고정으로 "화, 수, 목, 금, 토" 이고, 각 요일 내부의 시간대 순서는 코드 상단에서 리스트로 정의한다. 예:` 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

16/43 

25. 11. 30. 오후 1:45 

프로그램 - 주간 일정 포맷 안내 

 

```
   TIME_SLOTS = [
```

```
       "PM 01-03",
       "PM 03-05",
       "PM 05-07",
       "PM 07-09",
       "PM 08-10",
       "PM 12-02",
       "PM 02-04",
       "PM 04-05",
       "PM 05-06",
       "PM 06-08",
   ]
```

```
   - CSV에있는시간대가위리스트중어디에들어가는지기준으로정렬한다.
```

- `리스트에 없는 시간대가 들어오면, 마지막에 배치해도 된다.` 

`9. 최종 출력은 하나의 문자열로 만들어` 

```
   "weekly_schedule.txt" 파일로저장한다.
파일내용은그대로복사해서카카오톡단체방에붙여넣을수있는형식으로만든다.
```

## 이제 **주간 일정 포맷은 완전히 엔오원 방식으로 고정** 했다고 보면 돼. 

다음 단계에서 원하면: 

- 실제 예시 CSV 5줄짜리 만들어주고 

- 그걸 넣었을 때 **진짜로 어떤 텍스트가 나오는지** 까지 

- 한 번에 풀 버전으로 펼쳐줄게. 

여기서 한가지더 csv파일과 엑셀파일은 니가 읽어들이기 편하게 하기 위함이야? 내가 작업과보기가 편함이야? 

둘 다야 형. 근데 **역할이 완전 다르다** 고 보면 편해. 

## **1. 프로그램 입장: CSV가 제일 편함** 

파이썬이든 그래비티든, **코드 입장에서는 CSV가 제일 다루기 쉬움** 그냥 “텍스트 + 콤마(,)로 구분된 표”라서 

추가 설치 없이 바로 읽고 쓸 수 있어. 

그래서 내가 자꾸 

```
students_master.csv
```

이런 이름을 쓰는 이유가 

→ **“자동화 코드가 읽어먹기 편한 형식”**이라서 그래. 

즉, 

## **자동화 / 프로그램 = CSV를 좋아함** 

형이 CSV 파일을 눈으로 직접 보고 편하게 쓰라고 말하는 건 아님. 코드용 포맷이라고 생각하면 돼. 

## **2. 형 입장(작업·보기): 엑셀이 훨씬 편함** 

사람 눈으로 관리하고 수정하기에는 → **엑셀(또는 노션)** 이 훨씬 편해. 그래서 보통 구조는 이렇게 가는 게 제일 좋아: 

**1.** “형은 **엑셀 파일** 에서 수강생/스케줄을 관리한다.” 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

17/43 

25. 11. 30. 오후 1:45 

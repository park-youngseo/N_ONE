
아래 구조로 프로젝트를 만든다. 

text 코드 복사 

## `project/` 

```
 ├─ main.py
 ├─ fetcher.py          # 필름메이커스에서글/댓글수집 (그래비티가구현)
 ├─ post.py             # 게시글데이터구조 (코드제공)
 ├─ patterns.py         # 패턴/정규식상수 (코드제공)
 ├─ tagger.py           # 태그추출로직 (코드제공)
 ├─ cleaner.py          # 정제/필터/태깅 + dict 변환 (코드제공)
 ├─ saver.py            # CSV/JSON 저장 (코드제공)
 ├─ logger.py           # 로그출력/저장 (코드제공)
 ├─ logs/
 │   └─ crawler.log
```

https://chatgpt.com/c/69251d70-9f80-8324-ac8e-f14cdd82afa5 

532/633 

크롤러 제작 계획 

25. 11. 30. 오후 12:53 

```
 └─ output/
```

 

```
     └─ (결과 CSV/JSON 파일들)
```

- **제공 코드 6개 파일** 은 아래에 모두 들어 있다. 

- `fetcher.py` **/** `main.py` **/ exe 패키징** 은 그래비티가 이 지시문을 기반으로 생성해야 

- 한다. 

## **2. 크롤링·정제·태그 규칙 (요약)** 

이 규칙은 이미 확정된 것이므로, 코드에서 변경 금지. 

## **2-1. 수집 범위** 

- 사이트: filmmakers.co.kr 

게시판: 

- 엑터스 포럼 

- 연기자 정보방 

- 자유게시판 

기간: 

## **최근 3년** 

- 글 단위 수집: 

   - 제목, 본문, 댓글, 게시판 이름, 작성일자(문자열), 작성자 닉네임, 글 ID, URL(내부 확인용) 

## **2-2. 필터링 규칙** 

**전화번호 / 이메일 / URL → 전부 제거** 

- **“링크 드릴게요 / 링크 참고 / 링크 보내드릴게요” 같은 문장 → 문장 단위 삭제 광고/홍보 문장(문의주세요, DM 주세요, 프로모션, 이벤트 등) → 문장 단위 삭제 스탭(스태프) 모집 글 → 전체 게시글 제외** 

## **2-3. 텍스트 정제 규칙** 

탭/캐리지리턴 정리 

- 여러 칸 공백 → 1칸 

- 각 줄의 양 끝 공백 제거 

- ㅋㅋㅋㅋ, !!!!!, ㅠㅠㅠㅠ 같은 **3개 이상 반복 기호/이모티콘 → 2개로 압축** 

- 3줄 이상 연속 줄바꿈 → 2줄로 줄이기 

## **문장 의미는 변경하지 않고 “모양만” 정리** 

## **2-4. 태그 규칙** 

https://chatgpt.com/c/69251d70-9f80-8324-ac8e-f14cdd82afa5 

533/633 

25. 11. 30. 오후 12:53 

 

**==> picture [57 x 8] intentionally omitted <==**

**----- Start of picture text -----**<br>
크롤러 제작 계획<br>**----- End of picture text -----**<br>


심리/감정/환경 제약(시간·거리·돈·직장 등) 관련 표현이 있으면 → `심리` **태그** 

- 그 중에서도 극단적인 위험 표현(“죽고 싶…”, “자살”, “극단적 선택” 등)이 있으면 → **추가로** `심리위험` **태그** 

태그는 예: `"심리"` 또는 `"심리,심리위험"` 형태로 CSV에 저장 

## **3. 제공 코드 — 아래 6개 파일을 그대로 생성하라 (내용 수정 금지)** 

## **3-1.** `post.py` 

python 

코드 복사 

```
# post.py
from dataclasses import dataclass
from datetime import datetime
from typing importList, Optional
```

```
@dataclass
```

```
classPost:
"""
크롤러가수집한 '원본게시글' 데이터를담는구조체.
```

- `board_name: 게시판 이름 (엑터스 포럼 / 연기자 정보방 / 자유게시판 등)` 

- `post_id: 사이트 내부 게시글 고유 ID (문자열)` 

```
    - url: 원본글 URL (내부확인용)
```

- `title_raw: 제목 원문` 

- `content_raw: 본문 원문` 

- `comments_raw: 댓글 원문 리스트` 

- `created_at: 작성일 (datetime, 못 구하면 None)` 

```
    - writer: 작성자닉네임 (없으면 None)
    """
```

```
    board_name: str
    post_id: str
    url: str
    title_raw: str
    content_raw: str
    comments_raw: List[str]
    created_at: Optional[datetime]
    writer: Optional[str] = None
```

https://chatgpt.com/c/69251d70-9f80-8324-ac8e-f14cdd82afa5 

534/633 

크롤러 제작 계획 

25. 11. 30. 오후 12:53 

 

## **3-2.** `patterns.py` 

python 

코드 복사 

```
# patterns.py
import re
```

```
# ---------- URL / 이메일 / 전화번호패턴 ----------
URL_PATTERN = re.compile(r"https?://\S+|www\.\S+", re.IGNORECASE)
EMAIL_PATTERN = re.compile(r"[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}")
PHONE_PATTERN = re.compile(r"\b\d{2,3}-\d{3,4}-\d{4}\b")
```

```
# ---------- 링크언급 / 광고성문장힌트 ----------
LINK_SENTENCE_HINTS = [
"링크드릴게요",
"링크보내드릴게요",
"링크참고해주세요",
"아래링크참고",
"위링크참고",
"링크남깁니다",
]
AD_HINTS = [
"문의주세요",
"문의주시면",
"DM 주세요",
"디엠주세요",
"카톡주세요",
"카카오톡주세요",
"홍보",
"할인이벤트",
"이벤트진행중",
"프로모션",
"체험단모집",
]
```

```
# ---------- 스탭모집관련힌트 ----------
STAFF_HINTS = [
"스탭모집",
"스태프모집",
"촬영스탭구합니다",
"촬영스태프구합니다",
"현장스탭구해요",
```

https://chatgpt.com/c/69251d70-9f80-8324-ac8e-f14cdd82afa5 

535/633 

크롤러 제작 계획 

25. 11. 30. 오후 12:53 

 

```
"현장스태프구해요",
```

```
"스탭을모집합니다",
"스태프를모집합니다",
"스탭구합니다",
"스태프구합니다",
]
```

_`# ----------`_ `반복 기호` _`/`_ `이모티콘 압축용 패턴` _`---------#`_ `예` _`:`_ `ㅋㅋㅋㅋ` _`, !!!!!, ??!!??!!,`_ `ㅠㅠㅠㅠ 등 REPEAT_PUNCT_PATTERN = re.compile(r"([.!?~ㅋㅎㅠㅠㅠㅠㅠㅠ` 😊😢⭐ `]+)\1+")` 

```
# ---------- 심리 / 환경 / 위험표현힌트 ----------
```

```
# 일반심리상태 (불안, 자신감저하등)
PSYCH_HINTS = [
"불안", "초조", "두렵", "무섭", "걱정",
"포기하고싶", "그만두고싶", "좌절",
"자신감이떨어", "자신감이없", "자신감이안",
"의욕이없", "의욕이안나",
"무기력", "힘이안나", "하기가싫",
"멘탈이", "멘탈이무너", "멘탈나갔",
"우울", "침체", "허무",
]
```

```
# 환경/현실제약 (시간·거리·돈·직장등)
ENV_HINTS = [
```

```
"시간이없", "시간부족", "시간이부족",
"집이멀", "거리가멀",
"돈이없", "비용이부담", "수업료가부담",
"등록비가부담", "학원비가부담",
"직장때문에", "회사때문에", "알바때문에",
"스케줄때문에", "시간조율이안", "쉬는날이안맞",
]
```

```
# 극단적인위험표현
RISK_HINTS = [
"죽고싶", "죽고싶",
"자살", "극단적인선택",
"사는게힘들", "사는게힘들",
"모든걸끝내고싶", "끝내버리고싶",
"인생을포기", "포기하고싶을정도",
]
```

https://chatgpt.com/c/69251d70-9f80-8324-ac8e-f14cdd82afa5 

536/633 
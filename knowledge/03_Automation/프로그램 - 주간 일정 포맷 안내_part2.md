
프로그램 - 주간 일정 포맷 안내 

```
- 엔오원학원의수강생정보를하나의 "수강생마스터표"로모으고,
```

```
- 이표를바탕으로
  1) 주간수업일정안내문
  2) 매월결제확인 / 미납자안내문
을자동으로만들어주는파이썬프로그램을만든다.
```

```
[데이터소스]
- 기본입력파일: CSV 형식 (예: students_master.csv)
- 첫번째줄은컬럼이름(헤더)이다.
- 컬럼구성은아래와같다.
[수강생마스터표컬럼정의]
1) 이름 (텍스트)
   - 예: 김지훈
2) 연락처 (텍스트)
   - 예: 010-1234-5678
3) 카톡ID (텍스트)
   - 예: actor_jihoon
4) 현재수업 (텍스트또는선택)
   - 예시값:
     - 메커니즘 Class A
     - 메커니즘 Class B
     - 메커니즘 Class C
     - 메커니즘 Class D
     - 감정심화초급
     - 감정심화중급
     - 감정심화고급
     - THE Room
     - 기타(개인레슨)
5) 수업요일 (텍스트또는선택)
   - 예시값: 화, 수, 목, 금, 토
6) 수업시간 (텍스트또는선택)
   - 예시값:
     - PM 01-03
     - PM 03-05
     - PM 05-07
     - PM 07-09
7) 수업상태 (텍스트또는선택)
   - 예시값:
     - 진행
     - 휴식
     - 종료
     - 보류
8) 수강료 (숫자)
   - 단위: 원 (정수)
   - 예: 450000
9) 결제상태 (텍스트또는선택)
   - 예시값:
     - 완납
     - 부분
     - 미납
10) 메모 (긴텍스트)
    - 예: 12월부터감정심화추가예정
이 10개컬럼은프로그램에서고정된이름으로사용해도좋고,
영문필드명이필요하다면아래와같이매핑해도좋다.
```

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

6/43 

25. 11. 30. 오후 1:45 

프로그램 - 주간 일정 포맷 안내 

 

```
- 이름       → name
- 연락처     → phone
- 카톡ID    → kakao_id
```

- `현재수업   → current_class` 

- `수업요일   → weekday - 수업시간   → time_slot` 

- `수업상태   → status` 

- `수강료     → tuition - 결제상태   → payment_state - 메모       → memo` 

```
필요하다면 CSV 헤더는영문으로, 화면이나리포트출력은한글로사용해도된다.
```

```
[기능 1 – 주간수업일정안내문생성]
```

`1. 프로그램은 students_master.csv를 읽어서,` 

- `"수업상태 = 진행" 인 행만 사용한다.` 

`2. 요일(수업요일) → 시간대(수업시간) → 현재수업 → 이름 순으로 정렬한다.` 

`3. 출력 형식(예시):` 

- `[이번 주 레슨 안내]` 

```
(화)
PM 01-03 메커니즘 Class A – 김지훈, 이민수
PM 03-05 감정심화초급 – 박수진
...
```

```
(수)
PM 01-03 메커니즘 Class B – OOO, OOO
...
```

```
이처럼요일별로블록을나누고,
각요일안에서는시간순서대로출력한다.
```

`4. 요일 표기는 "화/수/목/금/토" 만 사용하고,` 

- `월/일/요일 계산은 이번 버전에서는 하지 않는다.` 

- `(추가 버전에서 날짜 계산을 넣을 수 있도록 코드 구조만 깔끔하게 만들어 둔다.)` 

`5. 최종 출력은 텍스트 파일(.txt) 또는 콘솔 출력 둘 다 가능하게 만든다.` 

- `예: weekly_schedule.txt 로 저장` 

- `[기능 2 – 결제/미납 안내문 자동 생성]` 

`1. 기준 규칙` 

- `결제 마감 기준: "매월 1주차 금요일까지 권장"` 

- `미납 안내 발송 기준: "1주차 일요일에 미납 안내문 생성"` 

`2. 프로그램 실행 시 "기준 월"을 입력 받을 수 있으면 좋다.` 

- `예: 2025-11 처럼 "YYYY-MM" 형태의 문자열` 

- `기준 월을 입력하지 않으면, 시스템 현재 날짜의 월을 사용한다.` 

`3. 결제상태 컬럼이 아래 값인 수강생만 필터링한다.` 

- `부분` 

- `미납` 

`4. 각 수강생마다 1줄짜리 안내 문장을 생성한다.` 

- `예시 포맷(한글):` 

   - `이름]님, [기준월] 레슨비 수강료 [수강료]원이 아직 완료되지 않아 안내드립니다."` 

- `예:` 

   - `김지훈님, 2025-11 레슨비 수강료 450000원이 아직 완료되지 않아 안내드립니다."` 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

7/43 

25. 11. 30. 오후 1:45 

프로그램 - 주간 일정 포맷 안내 

 

```
5. 출력방식:
```

- `전체 미납/부분 결제자 목록을 txt 파일로 저장 (예: payment_notice_2025-11.txt)` 

- `한 줄에 한 명씩, 위의 형식대로 문장을 출력한다.` 

```
[프로그램구조요구사항]
```

```
- 파이썬 3.x 기준으로동작하는단일스크립트파일로만든다.
```

```
  - 예: n_one_student_manager.py
```

```
- 필요한외부라이브러리가있다면최소한으로사용하고,
표준라이브러리(csv, datetime, pathlib 정도)를우선적으로사용한다.
```

```
- 주요함수예시 (이름은자유롭게변경가능)
```

```
  - load_students(csv_path) → 수강생목록로드
```

```
  - generate_weekly_schedule(students) → 주간안내텍스트생성
```

- `generate_payment_notices(students, 기준월) → 미납 안내 텍스트 생성` 

- `main() → 전체 흐름 제어` 

```
- 코드안에 "설정값" (예: CSV 파일경로, 요일순서, 시간대순서등)은
상수(CONFIG) 형태로위쪽에모아서적어둔다.
```

```
[출력요약]
```

```
- weekly_schedule.txt
```

```
  → 이번주수업일정안내문 (요일별/시간대별)
```

```
- payment_notice_YYYY-MM.txt
```

```
  → 기준월의미납/부분결제수강생안내문장모음
```

## — 확장(옵션) — 

- 위 설계에 **“월간 리포트(통계)”** 나 

- “수강생 상태 변화 로그 기록(입학·휴식·재등록)” 같은 기능을 추가하는 것도 가능하지만, 이건 형이 “확장 승인” 줄 때 별도로 붙일게. 

## 지금 단계에서 이 문서는 

**“그래비티에 넣으면, 수강생 마스터 표 + 주간 안내문 + 결제 알림을 한 번에 짜주는 설계서 v1”** 로 보면 돼. 

## 다음 단계에서는 

- 이 설계를 기준으로 **실제 예시 데이터 3~5명짜리 CSV 샘플** , 

- 그리고 형이 쓰는 **레슨 안내 멘트 스타일** 에 더 맞춘 출력 포맷 쪽으로 이어서 정리해줄 수 있어. 

3. 카톡ID는 필요없음/차라리 인스타 주소가 맞지 5.수업요일, 선택 타입이면 두과목을 듣고 일정이 각각 다른 날이면 어떻게 선택을 해야하지? 이 거 해결해야해 

이프로그램은 어떻게 정리가 되는지 자세하게 이야기해봐 

12s 동안 생각함 

좋아 형, 딱 걸린 포인트 두 개부터 정리할게. 

## **1. 카톡ID → 인스타 주소로 교체** 

https://chatgpt.com/g/g-p-68f8d8277a8881919e041e0e2d2a7c9d-peurogeuraem/c/6927bcb1-fda0-8320-b75d-4905d5b961de 

8/43 

25. 11. 30. 오후 1:45 

프로그램 - 주간 일정 포맷 안내 

 

## **2. 두 과목 수강 + 요일/시간 다른 경우 처리** 

**3.** 그 상태에서 **프로그램이 전체적으로 어떻게 움직이는지** 설명 

## **A단계｜데이터 구조 다시 정리 (형이 말한 문제 해결 버전)** 

## **1. 컬럼 수정 먼저** 

형이 말한 대로 고치면, 수강생 마스터 표는 이렇게 바뀌어: 

**1.** 이름 

**2.** 연락처 

**3.** 인스타주소 

**4.** 현재수업 

**5.** 수업요일 

**6.** 수업시간 

**7.** 수업상태 
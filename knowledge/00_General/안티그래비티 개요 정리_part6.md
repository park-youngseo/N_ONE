코드 복사 

```
### 역할정의
```

```
너는 '엔오원(N·ONE)'이라는연기학원을위한
```

- `[시장 리서치 + 크롤러 설계 전담 AI 에이전트]다.` 

```
### 최종목표
```

```
이프롬프트에포함된원문(니즈·원츠_01)을분석해서,
```

```
배우지망생시장에맞는다음결과물을만들어라.
```

`1. 시장 리서치 구조 정리` 

- `배우 지망생(입시 제외)의 심리·행동·수요 구조를, 크롤링/데이터 수집에 활용할 수 있는 형태로 재정리한다.` 

- `"어떤 사이트에서 / 어떤 정보를 / 어떤 기준으로" 모아야 하는지,` 

- `체크리스트와 우선순위로 뽑는다.` 

`2. 크롤러 설계서(필름메이커스 중심)` 

- `'필름메이커스 오디션 게시판'을 1차 타깃으로 한다.` 

- `필요하다면 네이버 카페(오디션 관련) 1~2곳을 예시로 포함해도 좋다.` 

- `사이트별로 다음을 정리한다:` 

https://chatgpt.com/c/69234a95-8ccc-8322-a990-08f5b9cebecf 

22/61 

25. 11. 30. 오후 12:55 

안티그래비티 개요 정리 

 

- `1) 기본 URL, 목록/상세 페이지 구조` 

- `2) 수집해야 할 필드(제목, 작성일, 마감일, 제작사/게시자, 카테고리, 링크, 지원 방법` 

- `3) 1년치 데이터를 안정적으로 모으기 위한 전략(페이지네이션·날짜 필터 등)` 

- `4) 중복 글 처리 기준(예: URL, 제목+작성일 조합)` 

`3. DB 설계 (배우 지망생 오디션 정보용)` 

- `위에서 정의한 필드들을 기반으로,` 

- `"오디션 정보"를 저장할 데이터베이스 구조를 설계한다.` 

- `최소 2개 이상 테이블 구조를 제안한다.` 

- `예) auditions, audition_sources, crawl_logs ...` 

- `각 테이블에 대해:` 

- `컬럼명, 타입, 설명, 인덱스(검색에 중요한 키)를 제안한다.` 

- `SQL CREATE TABLE 예시까지 작성한다.` 

`4. Python 크롤러 코드 뼈대` 

- `requests + BeautifulSoup4 기반의 크롤러 코드 뼈대를 작성한다.` 

- `대상: 일단 필름메이커스 오디션 게시판 1곳을 예시로 구현한다.` 

- `기능:` 

- `1) 지정한 기간(최근 1년)의 글 목록을 순회` 

- `2) 각 글에서 제목, 작성일, 마감일, 제작사/게시자, 카테고리, 링크, 지원 방법 일부를` 

- `3) 추출 결과를 Python dict 리스트 형태로 반환` 

- `코드 구조:` 

- `config 영역: BASE_URL, 기간 설정, 요청 헤더 등` 

- `fetch_list_page(url) 함수` 

- `parse_list(html) 함수` 

- `fetch_detail(url) 함수` 

- `parse_detail(html) 함수` 

- `run_crawler() 메인 함수` 

## `5. 안정성·차단 방지 설계` 

- `HTTP 429(Too Many Requests) 또는 403 같은 차단을 피하기 위한 전략을 설계한다.` 

- `예: 요청 간 랜덤 sleep, User-Agent 변경, 재시도 로직 등.` 

- `코드 안에 최소한의 방어 로직(딜레이, 재시도, 예외 처리)을 포함한다.` 

## `6. 결과물 포맷` 

```
   - 크롤러의최종출력은,
나중에 CSV/엑셀로저장하기쉬운형태(list[dict])로설계한다.
   - 예시: [{"title": "...", "date": "...", ...}, ...]
```

```
---
```

```
### 작업방식(중요)
```

```
1. 먼저, 아래원문텍스트(니즈·원츠_01)를자세히읽고,
```

https://chatgpt.com/c/69234a95-8ccc-8322-a990-08f5b9cebecf 

23/61 

25. 11. 30. 오후 12:55 

안티그래비티 개요 정리 

 

```
배우지망생의 '행동루프'와 '정보를보는관점'을파악해라.
```

`2. 그 이해를 바탕으로,` 

- `① 시장 리서치 구조 → ② 크롤러 설계서 → ③ DB 설계 → ④ Python 코드 뼈대 순서로 차례대` 

`3. 코드는 "사람이 나중에 수정/확장하기 쉽게" 함수와 상수로 나눠서 만드는 것을 우선한다.` 

`4. 한 번에 모든 걸 완벽하게 만들기보다는,` 

- `"동작 가능한 1차 버전"에 집중해라.` 

```
---
```

```
### 출력형식
```

```
아래순서대로출력해라.
```

- `1) [시장 리서치 요약]` 

- `수집해야 할 사이트 목록` 

- `각 사이트에서 수집할 필드` 

- `왜 이 필드들이 중요한지 (배우 지망생 심리와 연결해서)` 

- `2) [크롤러 설계서]` 

- `사이트별 구조 분석` 

- `수집 로직 요약` 

- `중복 처리/에러 처리 전략` 

- `3) [DB 설계]` 

- `테이블 구조 설명(자연어)` 

- `SQL CREATE TABLE 예시` 

- `4) [Python 크롤러 코드 뼈대]` 

- `주석이 포함된, 실행 가능한 예시 코드` 

```
---
```

```
### 참고용원문 (니즈·원츠_01 전체)
```

```
[텍스트시작]
```

```
[엔오원]
```

```
니즈와원츠_01
```

`0. 전제` 

- `[사실] 시장 정의: **배우 지망생(입시 지망생 제외)**` 

- `[사실] 핵심 심리코드: **사회적 인정·주목 욕구**가 크며, “인정=성공=수입”으로 인식` 

- `[사실] 목표 인식: **데뷔**가 인정의 증거이자 성공의 문` 

https://chatgpt.com/c/69234a95-8ccc-8322-a990-08f5b9cebecf 

24/61 

25. 11. 30. 오후 12:55 

안티그래비티 개요 정리 

 

- `[추론] 모든 행동은 “데뷔 확률을 높이려는 시도”로 수렴` 

- `[가정] 엔오원 메시지·구조는 위 심리 흐름을 **직접** 건드리는 방식이 **전환확률**이 높` 

- `# 1. 니즈 → 원츠 → 수요 (심리·행동 완전 매핑)` 

## `## 1-1. 니즈(Need)` 

- `[사실] **사회적으로 인정받고 주목받고 싶다.**` 

- `[추론] 인정이 곧 성공으로 해석되며, 돈은 결과적으로 따라온다고 믿음` 

- `[가정] “인정 경험”을 만들어주는 장치가 강력한 동기 유발원(권장됨)` 

- `## 1-2. 원츠(Want) — 데뷔를 원함` 

- `[사실] “연기를 해서 **데뷔**하고 싶다.”` 

- `[추론] “연기 → 데뷔 → 작품 지속 → 인지도 → 대중의 인정 → 성공 → 수입”의 직선 사고` 

- `[가정] ‘데뷔 전(前) 단계의 **보이는 증거**’ 제공이 중요(권장됨)` 

- `## 1-3. 원츠 기반 5대 행동(행동 근거 포함)` 

`1. **오디션 정보 획득**` 

- `커뮤니티/카페 가입, 홍보 미션 수행, 이메일 지원` 

- `심리: “정보가 곧 기회”` 

`2. **SNS·유튜브 업로드(자기 PR)**` 

- `연기 영상 공개, 노출 확대` 

- `심리: “보여야 선택받는다”` 

`3. **학원 등록(기대형)**` 

- `학습 목적 소수, 다수는 “캐스팅 동선 접근” 기대` 

- `심리: “학원=오디션 접근로”` 

`4. **성공 사례 콘텐츠 탐색**` 

- `유명 배우·감독의 오디션 방식·성공담 연구` 

- `심리: “패턴 복제 가능” 기대` 

`5. **단편·독립영화 참여(필모 구축)**` 

- `경력·이력서 확보, **관계자 노출 기대**(형 보충)` 

- `심리: “작품=존재 증명, 안도감”` 

- `## 1-4. 수요(Demand) 발생 트리거` 

- `[사실] **① 한계 인식형(극소수)**: 실력·기회 부족을 자각했을 때 결제` 

https://chatgpt.com/c/69234a95-8ccc-8322-a990-08f5b9cebecf 

25/61 

25. 11. 30. 오후 12:55 

안티그래비티 개요 정리 

 

- `[사실] **② 기회 접근형(절대다수)**: **캐스팅·데뷔 연결 루트**를 발견했을 때 결제` 
